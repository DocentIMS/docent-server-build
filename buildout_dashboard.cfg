[buildout]
extends =
    https://dist.plone.org/release/6.2.0/versions.cfg
    https://dist.plone.org/release/6.2.0/versions-ecosystem.cfg

# enable checkout from git 
# and prevent running plone as root
extensions =
    mr.developer
    buildout.sanitycheck

[buildout]
parts =
    instance
#    i18ndude
    zopepy
#    test
#    repozo
#    backup
#    backupcronjob
    zopescripts

    
auto-checkout =
    DocentIMS.dashboar

[instance]
recipe = plone.recipe.zope2instance
# user = admin:admin
# http-address = 8080

eggs =
    Plone
    DocentIMS.dashboard 


versions=versions

sources-dir = src
event-log-max-size = 5 MB
event-log-old-files = 5
access-log-max-size = 20 MB
access-log-old-files = 5

[zopescripts]
# generates the Zope and WSGI specific scripts
recipe = zc.recipe.egg
eggs =
    Zope
    Paste
    Plone


# zopepy commands allows you to execute Python scripts using a PYTHONPATH
# including all the configured eggs
[zopepy]
recipe = zc.recipe.egg
eggs = ${instance:eggs}
interpreter = zopepy
scripts = zopepy
   
[sources]
DocentIMS.dashboard = git git@github.com:DocentIMS/DocentIMS.dashboard.git

[versions]
z3c.jbot=3.1
z3c-jbot=3.1
